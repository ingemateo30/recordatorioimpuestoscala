export async function enviarWhatsApp(numero: string, mensaje: string) {
    console.log(`📲 Enviando WhatsApp a ${numero}: ${mensaje}`);
    // Aquí debes integrar la API de WhatsApp (Twilio, Meta, etc.)
  }